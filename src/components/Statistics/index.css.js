import styled from 'styled-components';
import { MainContainerCss } from '../shared/styles.css';

export const StatisticsContainerCss = styled(MainContainerCss)`
	background: var(--clr-main);
`;

export const StatisticsErrorCss = styled.p`
	color: var(--clr-white);
`;
